## 요약

Slice B (배송기사 배치 링크 + 자동 SMS) 5-team 디스패치 통합. 사용자 명시: 카톡 자동 게이트웨이가 아닌 **기사에게 SMS 1건 자동 발송 → 기사가 모바일 링크로 인수자 서명 받음 → 서명 완료 슬립을 기사가 인수자에게 직접 공유** 모델.

월 추정 발송량 150~450건 (월 1.5~4.5천원, Solapi).

## 사용자 결정 사항 (확정)

| ID | 결정 |
|---|---|
| N1 | SMS 발송 트리거 — **관리자 수동 클릭** (오발송 방지) |
| N2 | 그룹화 단위 — **driverPhone + batchDate 자동** + 관리자가 분리/병합 가능 |
| N3 | SMS 게이트웨이 — **Solapi** (HMAC-SHA256 v4 API) |
| N4 | 인수자 공유 — **Web Share API** + 폴백 clipboard 복사 (Slice C) |
| N5 | 토큰 유효기간 — **배송일 +1일 자동 만료** |

## 5-team 산출

### Designer (5번째 팀)
- 6 spec 파일 (`docs/design/notification-slice-B/`) — README/wireframes/tokens/components/ux-flow/mobile-spec
- 4 mock HTML + 4 Edge headless 캡처 (`docs/design/notification-slice-B/mocks/`, `screenshots/`)
- 신규 토큰 3 그룹 (`--phone-input-*`, `--copy-button-*`, `--batch-list-row-*`)

### BE (slip-service 확장 + DeliveryBatch 신규)
- **Slip 도메인 확장**: driverName/driverPhone/deliveryBatchId 3 필드 (nullable) + `editHeader()` 4→6 args
- **DeliveryBatch 신규 도메인** (`com.samhanair.logis.slip.delivery.*`): entity + Repository + Service + 7 admin endpoint + 1 공개 endpoint (PublicSlipController, 410 GONE)
- **Solapi 통합**: SmsGateway interface + SolapiSmsGateway (HMAC-SHA256 v4) + MockSmsGateway + SmsConfig (프로파일 분기)
- **Flyway**: V3 (slips 3 컬럼 + 2 partial index) + V4 (delivery_batches + UK + FK)
- **API Gateway**: `/api/delivery-batches/**` + `/api/public/**` 라우트 + SecurityConfig `/public/**` permitAll
- 라이프사이클 메서드 **무변경** → 8 라이프사이클 IT 시나리오 회귀 0
- 단위 90건 PASS (76 기존 + 14 DeliveryBatchTest + 14 DeliveryBatchServiceTest)

### FE (디자인 시스템 17→18 컴포넌트 + 링크발송 메뉴)
- 신규 컴포넌트: `<PhoneInput>` (자동 하이픈, KOREAN_MOBILE_PHONE_PATTERN), `<CopyButton>` (clipboard + execCommand 폴백), `<BatchStatusCell>` (sent/unsent 분기)
- 신규 라우트 `/sales/link-dispatch` — LinkDispatchListPage + BatchDetailModal
- 사이드바 "재고이동" 아래 "**링크발송**" 메뉴 추가
- SlipFormPage / SlipDetailPage 헤더에 driverName + driverPhone 2 필드
- DispatchView 인쇄 — 용달기사 결재란 셀에 driverName 자동 표시 (1줄)
- API 클라이언트 `api/delivery.ts` 신규 + mock.ts 4 배치 시드

### QA (IT 18 시나리오 + fixtures + 권한 매트릭스)
- IT 시나리오 18개 (DeliveryBatchControllerIT 11 + PublicSlipControllerIT 3 + SlipDriverFieldsIT 4)
- fixtures.http 시나리오 13~17 (5 블록)
- 7-tier 권한 매트릭스 (MASTER/MANAGER/SALES/WAREHOUSE/INVENTORY/ACCOUNTANT/AUDITOR)
- BE Layer 4 시그니처 가정 표 (Plan §3.3 그대로)
- UUID 비공개 가드 검증 (공개 응답 jsonPath 에 batch.id/slip.id 부재)
- ※ PM 통합 시 QA 의 중복 IT (`slip.it.*`, SmsResult import 누락) 3개 삭제, BE 의 `slip.delivery.it.*` 채택

### DevOps (env + Flyway 검증 + CI 영향 0)
- 신규 env 4개 (SOLAPI_API_KEY/SECRET/SENDER_PHONE/BASE_URL)
- `infrastructure/.env.example` + `infrastructure/env-templates/slip-service.env`
- Flyway V3/V4 PgSQL 16 컨테이너 시연 검증 (partial index + UK + FK 모두 정상)
- CI 변경 0 (test 프로파일 MockSmsGateway → SOLAPI 미주입 OK)
- README 환경변수 섹션 신설

## QA 캡처 (Designer mock)

### 1. 관리자 — 링크발송 화면
![LinkDispatchListPage](https://raw.githubusercontent.com/ewoo14/SamhanLogis/bcdbea0b3aceefb9d2eab756b4a2bd8dacffdb16/docs/design/notification-slice-B/screenshots/01_link_dispatch_list.png)

### 2. BatchDetailModal (슬립 추가/제거)
![BatchDetailModal](https://raw.githubusercontent.com/ewoo14/SamhanLogis/bcdbea0b3aceefb9d2eab756b4a2bd8dacffdb16/docs/design/notification-slice-B/screenshots/02_link_dispatch_modal.png)

### 3. SlipForm — driverName/driverPhone 입력
![SlipForm driver](https://raw.githubusercontent.com/ewoo14/SamhanLogis/bcdbea0b3aceefb9d2eab756b4a2bd8dacffdb16/docs/design/notification-slice-B/screenshots/03_slip_form_driver_fields.png)

### 4. 모바일 공개 페이지 (Slice C 통합 지점)
![Mobile public batch](https://raw.githubusercontent.com/ewoo14/SamhanLogis/bcdbea0b3aceefb9d2eab756b4a2bd8dacffdb16/docs/design/notification-slice-B/screenshots/04_mobile_public_batch.png)

## 검증

```
./gradlew :services:slip-service:compileTestJava PASS
./gradlew :services:slip-service:test PASS — 단위 90건 (76 기존 + 14 + 14 신규)
./gradlew :services:slip-service:assemble + :services:api-gateway:assemble PASS
design-system: npm run build PASS — style.css 35.75kB / index.js 57.67kB
desktop: npm run typecheck + lint PASS
PgSQL 16 컨테이너 V1→V2→V3→V4 순차 적용 시연 PASS (DevOps)
```

## 회귀 위험 0

- Slip 도메인 라이프사이클 메서드 (save/send/accept/process/inspect/complete/...) **무변경**
- 8 라이프사이클 IT 시나리오 영향 없음
- driverName/driverPhone/deliveryBatchId 모두 nullable + 기본 NULL → 기존 데이터 호환
- DeliveryBatch 신규 — 기존 도메인 무영향
- Docker compose / RabbitMQ / 기존 7 서비스 무영향 (DevOps 검증)

## Layer 4 도메인 메서드 의미 정렬 (PM 검증)

| 메서드 | from status | to status | 부수효과 |
|---|---|---|---|
| `DeliveryBatch.create(driver, date, slips)` | — | (initial) | batchToken base64url 64자 + tokenExpiresAt = batchDate+1일 |
| `markSmsSent()` | smsSentAt=null | smsSentAt=now | Solapi 호출 성공 후만 |
| `markSmsFailed(error)` | smsSentAt=null | smsSentAt=null + smsLastError | 재시도는 사용자 재클릭 |
| `addSlip(slip)` | unchanged | unchanged | slip.deliveryBatchId 갱신 (다른 batch 소속이면 자동 이전) |
| `removeSlip(slip)` | unchanged | unchanged | slip.deliveryBatchId = null |
| `Slip.editHeader(... driverName, driverPhone)` | DRAFT/SAVED | unchanged | 6 args 확장 |
| `Slip.inspect(...)` (기존) | INSPECTING | COMPLETED | **변경 없음** (Layer 4 정렬 유지) |

QA Report §3.1 의 시그니처 가정과 **1:1 일치 확인**.

## 후속 슬라이스

- **Slice C** (2주): 모바일 공개 페이지 서명 캡처 (HTML5 Canvas + SHA-256 + 표준 전자서명) + Web Share API 인수자 공유
- **Phase 5 Notification Service 분리**: Slack/Teams 등 Webhook 채널 추가 시점에 별도 서비스로 분리 (현재는 slip-service 내부 SMS 로 충분)

## Test plan

- [x] BE compile + 단위 90건 PASS
- [x] FE design-system build + desktop typecheck + lint PASS
- [x] PgSQL Flyway V3/V4 시연 검증 (DevOps)
- [x] Layer 4 도메인 메서드 의미 정렬 (PM 검증)
- [x] UUID 비공개 가드 (공개 응답)
- [x] 회귀 가드 (라이프사이클 메서드 무변경)
- [ ] CI 자동 실행 (Linux Docker IT)
- [ ] 머지 후 Slice C 진입 (모바일 서명)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
