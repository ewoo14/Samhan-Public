package com.samhanair.logis.product.domain;

/** 사양 입력 타입 — NUMBER(숫자), DIMENSION(가로x높이x깊이), RANGE(최소/정격/최대), TEXT(자유 입력). */
public enum SpecKeyValueType {
    NUMBER,
    DIMENSION,
    RANGE,
    TEXT
}
