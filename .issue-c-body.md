## 슬라이스 개요

Slice C (모바일 전자서명) + Slice C2 (배송기사 서명 추가 캡처) — Slice B (배송기사 배치 링크 + Solapi SMS) 위에 다음 기능 추가:

1. **HTML5 Canvas 서명 캡처** (vanilla, 의존성 zero, ~5KB gzip)
2. **SHA-256 해시 + 서버 timestamp + 토큰 추적성** = 전자서명법 §3② "일반 전자서명" 민사상 추정 효력
3. **인수자 share 토큰** (30일 만료, base64url 64자)
4. **배송기사 서명 캡처** (Slice C2 — 인수자 서명과 동일 패턴)
5. **DispatchView 인쇄에 양측 서명 PNG 자동 삽입**
6. **MASTER only 무효화 + audit 로그**
7. **공개 endpoint 보안 헤더 6종** (CSP/HSTS/X-Frame/X-Robots 등)

## 사용자 결정 사항

| Q | 결정 |
|---|---|
| Q1 도메인 모델 | C안 (Slip 5필드 + audit 이력 별도 테이블) |
| Q2 PNG 저장 | A안 DB bytea (월 1만건 임계 후 MinIO) |
| Q3 서명 시 status | 무변경 (직교 메타) |
| Q4 share 만료 | 30일 |
| Q5 Web Share API | title/text/url 3필드 |
| Q6 RFC 3161 TSA | Phase 6 deferred |
| Q7 무효화 권한 | MASTER only + 감사 로그 |
| Q8 sign 도메인 | desktop 앱 mock + Phase 5 nginx |

## 5-team 디스패치 + Slice C2 follow-up

- Designer (`7e21bc6`): 6 spec + 4 mock + 4 Edge headless 캡처
- BE (`77b23af`): Slip 7필드 + audit + 4 endpoint + V5
- DevOps (`a0c72cc`): Flyway V5 PgSQL 시연 + 보안 헤더 6종
- QA (`c63f60e`): IT 14 시나리오 + fixtures + Layer 4 정렬
- FE (`639a675`): SignaturePad/SignatureViewer + mobile mock 라우트 + DispatchView 통합
- Slice C2 (`8aade22`): 배송기사 4 필드 + endpoint + 슬립→전표 명칭 통일
- 양식 정정 (`e2ee84c`): @Lob 제거 (CI fix) + 이름 미노출 / 가운데 정렬 / 책임 박스 / 화면 높이 자동

## 회귀 위험 0
- Slip 라이프사이클 메서드 무변경
- 신규 컬럼 모두 nullable
- 기존 8 라이프사이클 IT 영향 X
- 보안 헤더 추가만 (라우팅 변경 X)

## 관련 PR
- PR #23

## TM/PM 승인 절차
이 Issue 의 코멘트로 TM agent 승인 + PM 승인 (CI 빌드+테스트 GREEN 확인 후) → 개발책임자 머지 진행.
