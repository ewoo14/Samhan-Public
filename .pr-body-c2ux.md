## 요약

PR #23 (signature-slice-C/C2) 잔여 작업 — 모바일 전자서명 페이지의 UX 강화:

1. **2-step 흐름**: ① 배송기사 서명 → ② 인수자 서명 (자동 전환)
2. **캔버스 fullscreen UX**: 서명 영역 클릭 → 전체화면 모달 → 하단 [완료] 버튼

관련 Issue: #25

## 사용자 명시 요구
- "배송기사 서명도 당연히 캡처해서 첨부 필요함" — BE 는 PR #23 에서 완료, FE 흐름은 본 슬라이스
- "서명 칸은 클릭시 자동 전체화면으로 키워서 서명하기 용이하게 하고 하단 완료 버튼을 눌러서 해당 서명이 들어가도록 처리"

## 변경 사항

### MobileSignaturePage 2-step
- `stage: 'driver' | 'recipient'` 신규 state
- 1단계 (driver): 인수자명 input 미표시, 캔버스만 + [서명 완료 (다음: 인수자)] 버튼 → POST `/driver-signature` → 자동 전환
- 2단계 (recipient): 인수자명 input + 캔버스 + [서명 완료] → POST `/signature` → share 페이지 navigate
- 단계 indicator: `① 배송기사 서명 → ② 인수자 서명` (현재 단계 highlight, 완료 단계 ✓)

### 캔버스 fullscreen 모달
- embed 캔버스 클릭 → `position: fixed inset: 0 z-index: 9999` 모달 진입
- viewport 92% × 60% 크게 확장 → 모바일에서 서명 용이
- 하단 [다시 서명] / [완료] 버튼 grid → [완료] 클릭 시 모달 닫기 (서명 데이터 SignaturePad ref 보존)
- body `overflow: hidden` (스크롤 잠금)
- 실제 BE 전송은 embed 의 [완료] 버튼 (단계별 분기)
- 빈 캔버스 시 안내문구: "손가락으로 서명하시려면 클릭하세요"

### CSS 신규
- `.m-stage-indicator` — ① ② 진행 표시 (active=파랑, done=녹색 ✓)
- `.m-canvas-wrap.is-embed` — cursor pointer + hover outline
- `.m-canvas-wrap.is-fullscreen` — overlay (fixed)
- `.m-canvas-fullscreen-actions` — grid 하단 버튼
- `.m-canvas-hint` — 빈 캔버스 안내문

## 캡처

![모바일 서명 페이지 (1단계 배송기사)](https://raw.githubusercontent.com/ewoo14/SamhanLogis/9a28bfdb0449baca5a6bf7518f28ffc58f094760/docs/design/signature-slice-C/screenshots/01_mobile_signature_page.png)

상단 단계 indicator (① 배송기사 서명 → ② 인수자 서명) + "배송기사 서명" 라벨 + 캔버스 hover hint + "서명 완료 (다음: 인수자)" 버튼.

## 검증

```
desktop: npm run typecheck PASS
desktop: npm run lint — 0 errors (1 pre-existing warning)
```

## BE 영향 0

- 기존 PR #23 의 `recordDriverSignature()` / `recordSignature()` API 그대로 사용
- 새 endpoint 추가 X

## 회귀 위험 0

- `<SignaturePad>` 디자인 시스템 컴포넌트 무변경
- DispatchView 인쇄 무변경
- 타 라우트 무영향

## Test plan

- [x] typecheck + lint PASS
- [x] mock 01 캡처 갱신 (1단계 시각화)
- [ ] CI 자동 실행
- [ ] 머지 후 mobile 실 디바이스 검증 (Phase 5 nginx 분리 후)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
