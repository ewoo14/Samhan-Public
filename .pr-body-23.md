## 요약

Slice C (모바일 전자서명) — Slice B 위에 **HTML5 Canvas 서명 캡처 + SHA-256 해시 + 인수자 share 토큰 + DispatchView 인쇄 통합** 추가. 5-team 디스패치.

전자서명법 §3② "일반 전자서명" — Canvas PNG + SHA-256 + 서버 timestamp + 토큰 추적성으로 **민사상 추정 효력** 확보 (PKI/공인인증/RFC 3161 TSA 는 Phase 6+ deferred).

## 사용자 결정 사항 (확정)

| Q | 결정 |
|---|---|
| Q1 도메인 모델 | **C안** (Slip 5필드 + audit 이력 별도 테이블) |
| Q2 PNG 저장 | **A안** DB bytea (월 30MB / MinIO 임계 1만건) |
| Q3 서명 시 status | **무변경** (직교 메타) |
| Q4 share 만료 | **30일** (BE 영구 보존 + share token 만 30일) |
| Q5 Web Share API | **`{title, text, url}` 3필드** (iOS/Android 호환) |
| Q6 RFC 3161 TSA | **Phase 6 deferred** |
| Q7 무효화 권한 | **MASTER only + 감사 로그** |
| Q8 sign 도메인 | **현 슬라이스: desktop 앱 mock** + Phase 5 nginx |

## 5-team 산출

### Designer (5번째 팀, `7e21bc6`)
- 6 spec (`docs/design/signature-slice-C/`) — README/wireframes/tokens/components/ux-flow/mobile-spec
- 4 mock + 4 캡처 (Edge headless)
- 신규 토큰 그룹 (`--signature-canvas-*`, `--signature-meta-*`, `--slip-signature-*`, `--print-signature-*`)

### BE (`77b23af`)
- **Slip 도메인 확장**: 7필드 (signedAt, signerName, signaturePng BYTEA, signatureHash, signatureChannel, signatureShareToken, signatureShareExpiresAt) + 4 도메인 메서드 (`recordSignature`, `invalidateSignature`, `isSignatureShareExpired`, `isSigned`)
- **신규 entity**: `SlipSignatureAudit` (RECORD/INVALIDATE 2종 action) — 무결성 입증
- **API 4 신규**: 공개 (POST 서명 저장 + GET 인수자 view) + admin (GET 조회 MANAGER/MASTER + DELETE 무효화 MASTER only)
- **검증**: 서버 SHA-256 재계산 → mismatch 400, PNG ≤50KB, signerName 1~50자
- **Flyway V5**: slips 7 컬럼 + slip_signature_audit 테이블 + partial UNIQUE index
- 단위 38건 PASS (도메인 21 + Mockito 17), 라이프사이클 메서드 무변경

### FE (`639a675`)
- 디자인 시스템 신규 컴포넌트: **`<SignaturePad>`** (vanilla canvas, 의존성 zero, pointer/touch + passive: false + devicePixelRatio), **`<SignatureViewer>`** (read-only PNG + 메타)
- desktop 모바일 mock 라우트 2건: `/mobile/d/:token/s/:slipNo` (서명 페이지) + `/mobile/share/:shareToken` (인수자 view)
- SlipDetailPage: 전자서명 정보 카드 + MASTER only 무효화 modal (reason ≥10자)
- DispatchView 인쇄: 인수자 셀 안 PNG `<img>` + 메타 (CSS-only, max-h 18mm + object-fit contain)
- API client `api/signature.ts` 4 함수 + mock 시드 + Web Crypto SHA-256

### QA (`c63f60e`)
- IT 시나리오 14개 설계 (PublicSignatureControllerIT 9 + SlipSignatureAdminIT 5)
- fixtures.http 시나리오 18~22 (5 블록)
- 7-tier 권한 매트릭스 + UUID 비공개 가드 검증
- BE Layer 4 시그니처 가정 표 (PR #22 회고 적용 — IT 자체 작성 X, 시나리오 설계만)

### DevOps (`a0c72cc`)
- Flyway V5 PgSQL 16 컨테이너 V1→V5 시연 PASS (BYTEA + slip_signature_audit + partial UNIQUE)
- 보안 헤더 6종 (CSP / X-Frame-Options / X-Content-Type-Options / HSTS / Referrer-Policy / X-Robots-Tag) + Permissions-Policy → `PublicSecurityHeaderFilter` 신규
- PNG bytea 저장 영향 (12개월 ~360MB, pg_dump --format=custom 의무)
- Phase 5 nginx deferred (`infrastructure/nginx/conf.d/sign.conf` draft)

## QA 캡처 (Designer mock)

### 1. 모바일 서명 페이지
![Signature page](https://raw.githubusercontent.com/ewoo14/SamhanLogis/cb927e425954afb5b05dba99cd35c7e56d53a94c/docs/design/signature-slice-C/screenshots/01_mobile_signature_page.png)

### 2. 서명 완료 후 [공유] 버튼
![Signed complete](https://raw.githubusercontent.com/ewoo14/SamhanLogis/cb927e425954afb5b05dba99cd35c7e56d53a94c/docs/design/signature-slice-C/screenshots/02_mobile_signed_complete.png)

### 3. 인수자 share view (서명 PNG)
![Recipient view](https://raw.githubusercontent.com/ewoo14/SamhanLogis/cb927e425954afb5b05dba99cd35c7e56d53a94c/docs/design/signature-slice-C/screenshots/03_recipient_share_view.png)

### 4. DispatchView 인쇄 (서명 PNG 통합)
![Dispatch print](https://raw.githubusercontent.com/ewoo14/SamhanLogis/cb927e425954afb5b05dba99cd35c7e56d53a94c/docs/design/signature-slice-C/screenshots/04_dispatch_print_with_signature.png)

## 검증

```
./gradlew :services:slip-service:compileTestJava PASS
./gradlew :services:slip-service:test PASS — 단위 128건 (90 기존 + 38 신규)
design-system: npm run build PASS — gzip 17.76 kB (75 modules)
desktop: npm run typecheck PASS / lint 0 errors
PgSQL 16 V1→V5 시연 PASS (DevOps Flyway)
```

## Layer 4 도메인 메서드 의미 정렬

| 메서드 | from status | to status | 부수효과 |
|---|---|---|---|
| `Slip.recordSignature(signerName, png, hash, channel)` | INSPECTING/COMPLETED/SHIPPING | unchanged | 7필드 갱신 + audit log INSERT + shareToken 발급 (30일 만료) |
| `Slip.invalidateSignature(reason, actor)` | signedAt!=null | unchanged | 7필드 NULL + audit log INSERT (action=INVALIDATE) |
| `Slip.isSignatureShareExpired(now)` | — | — | shareExpiresAt < now 검증 |
| `Slip.isSigned()` | — | — | signedAt != null |
| 기존 lifecycle (save/send/.../inspect/complete/...) | — | — | **변경 없음** (직교 메타) |

QA `qa-report.md` §3 의 시그니처 가정과 **1:1 일치 확인**.

## 회귀 위험 0

- Slip 도메인 라이프사이클 메서드 무변경 → 기존 IT 시나리오 영향 0
- 신규 7필드 모두 nullable + 디폴트 NULL → 기존 데이터 호환
- DispatchView 인쇄: 미서명 시 기존 라벨 유지 (조건부 PNG 추가만)
- SecurityConfig `/public/**` permitAll → 신규 endpoint 자동 포함 (의도)
- Docker compose / RabbitMQ / 기존 8 서비스 무영향

## UUID 비공개 가드

- 공개 endpoint (`POST/GET /public/.../signature`) 응답에 `slipId`, `batchId`, `signatureId` UUID 미노출
- 표시되는 비즈니스 식별자: slipNo, signerName, signedAt, hash 앞 8자
- 모바일 mock 라우트도 token / shareToken (base64url) 만 URL path 에 노출

## 보안 헤더 (DevOps `PublicSecurityHeaderFilter`)

`/public/**` 응답 헤더:
- `Content-Security-Policy: default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'`
- `X-Frame-Options: DENY`
- `X-Content-Type-Options: nosniff`
- `Strict-Transport-Security: max-age=31536000; includeSubDomains`
- `Referrer-Policy: no-referrer`
- `X-Robots-Tag: noindex, nofollow`
- `Permissions-Policy: camera=(), microphone=(), geolocation=()`

## 추가 hotfix (PR #23 follow-up)

### `d5622b7` — CI fail 74건 정정
- `Slip.signatureShareToken @Column(unique=true)` 제거 — V5 partial UNIQUE INDEX (NULL 허용) 와 mismatch → Hibernate validate SchemaManagementException → ApplicationContext 부팅 실패 → IT 74건 fail
- 모바일 mock 캡처 우측 잘림 정정 (480px 재캡처)

### `8aade22` — 배송기사 서명 캡처 + 슬립→전표 명칭 통일
- BE: Slip 4 신규 필드 (driverSignedAt/driverSignaturePng/driverSignatureHash/driverSignatureChannel) + recordDriverSignature 도메인 메서드 + V6 마이그레이션
- BE: 신규 endpoint `POST /public/batches/{token}/slips/{slipNo}/driver-signature` (no auth)
- BE: SignatureAuditAction.RECORD_DRIVER 신규 + recordDriver 팩토리
- FE: SlipDetail interface 4 필드 + signature.ts recordDriverSignature() 함수
- FE: DispatchView 인쇄에서 용달기사 셀에 driverSignaturePng `<img>` 자동 표시
- 슬립 → 전표 사용자 가시 문자열 일괄 치환 (8 파일)

## 후속 슬라이스

- **Slice C2 잔여**: MobileSignaturePage 2-step 흐름 (기사 → 인수자) + 서명 캔버스 fullscreen UX
- **Phase 5 web app**: sign.samhan-air.com nginx 분리 + Slack/Teams Webhook 채널
- **Phase 6**: MinIO PNG 마이그 (월 1만건 초과 시) + RFC 3161 TSA + 공인인증/PKI

## Test plan

- [x] BE compile + 단위 128건 PASS
- [x] FE design-system build + desktop typecheck + lint PASS
- [x] PgSQL Flyway V5 시연 검증 (DevOps)
- [x] Layer 4 도메인 메서드 의미 정렬 (PM 검증, QA 가정 ↔ BE 산출 1:1)
- [x] UUID 비공개 가드 (공개 응답)
- [x] 보안 헤더 6종 + Permissions-Policy (DevOps)
- [x] 회귀 가드 (라이프사이클 메서드 무변경)
- [ ] CI 자동 실행 (Linux Docker IT 18건)
- [ ] 머지 후 Phase 4 진입 결정 (Accounting Service vs 다른 우선순위)

🤖 Generated with [Claude Code](https://claude.com/claude-code)
