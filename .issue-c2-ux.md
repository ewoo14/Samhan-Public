## 슬라이스 개요

Slice C2-UX (PR #23 잔여) — 모바일 전자서명 페이지의 UX 개선:

1. **2-step 흐름**: ① 배송기사 서명 → ② 인수자 서명 (단계 표시 + 자동 전환)
2. **캔버스 전체화면**: 서명 영역 클릭 시 fullscreen 모달 + 하단 [다시 서명]/[완료] → 닫으면 서명 보존

## 사용자 명시 요구
- "배송기사 서명도 당연히 캡처해서 첨부 필요함" (BE 는 PR #23 에서 완료, FE 흐름은 본 슬라이스)
- "서명 칸은 클릭시 자동 전체화면으로 키워서 서명하기 용이하게 하고 하단 완료 버튼을 눌러서 해당 서명이 들어가도록 처리"

## 구현
- **MobileSignaturePage 2-step**:
  - stage 'driver' (인수자명 input 미표시) → POST `/driver-signature` → stage 'recipient' 자동 전환
  - stage 'recipient' (기존 흐름 유지) → POST `/signature` → share 페이지 이동
  - 단계 표시 (① ✓ → ②) — 시각적으로 진행 상황 명확
- **Fullscreen 캔버스**:
  - embed 캔버스 클릭 → `position: fixed inset: 0 z-index: 9999` 모달 → viewport 92% × 60%
  - 하단 [다시 서명] / [완료] 버튼 → [완료] 클릭 시 모달 닫기 (서명 데이터 SignaturePad ref 에 보존)
  - 실제 BE 전송은 embed 의 [완료] 버튼 (단계별)
  - body scroll 잠금 (overflow: hidden)
- **CSS 신규**:
  - `.m-stage-indicator` (① ② 표시)
  - `.m-canvas-wrap.is-embed` (클릭 가능 visual + hover outline)
  - `.m-canvas-wrap.is-fullscreen` (모달 overlay)
  - `.m-canvas-fullscreen-actions` (하단 버튼 grid)

## 변경 파일
- `clients/desktop/src/renderer/routes/MobileSignaturePage.tsx` — 2-step + fullscreen 흐름
- `clients/desktop/src/renderer/styles/global.css` — 신규 CSS
- `docs/design/signature-slice-C/mocks/01_mobile_signature_page.html` — mock 갱신 (1단계 시각화)
- `docs/design/signature-slice-C/screenshots/01_mobile_signature_page.png` — Edge 재캡처

## 검증
- desktop typecheck PASS
- design-system 컴포넌트 무변경 (`<SignaturePad>` 재사용)

## BE 영향 0
- 기존 PR #23 의 `recordDriverSignature()` API 그대로 사용
- 새 endpoint 추가 X

## 회귀 위험 0
- `<SignaturePad>` 컴포넌트 무변경
- DispatchView 인쇄 무변경
- 타 라우트 무영향

## TM/PM 승인
이 Issue 의 코멘트로 TM 검토 + PM 승인 (CI 빌드+테스트 GREEN 확인) 후 머지.
