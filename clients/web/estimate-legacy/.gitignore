node_modules/
.env
.env.local
.env.*.local
*.log
npm-debug.log*

# 변환 입력본 — legacy 원본 파일 복사 (gitignore: 원본은 migration/source/scripts/ 가 source of truth)
lib/_legacy-code-raw.js

# 빌드 산출물 (별도 commit — 본 라인 제거 시 generated 파일 commit 가능)
# lib/code.js — commit 함 (production 배포 결과물)
# views/index.ejs — commit 함
# public/assets/ — commit 함

# Playwright
test-results/
playwright-report/
playwright/.cache/
